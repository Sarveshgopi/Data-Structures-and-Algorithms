/* This is just a code snippet   */

class solution
{
    public:
        void wave_array(int *arr,int size)
        {
            int index,temp;
            for(index=0;index<(size-1);index+=2)
            {
                  temp=arr[i];
                  arr[i]=arr[i+1];
                  arr[i+1]=temp;
            }
       }
   };
        
